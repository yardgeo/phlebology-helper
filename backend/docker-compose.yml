version: '3.3'
services:
#  db-phleb:
#    image: postgres
#    ports:
#      - "5433:5432"
#    environment:
#      - POSTGRES_USER=admin
#      - POSTGRES_PASSWORD=admin
#      - POSTGRES_DB=viewer
#    networks:
#      - phlebology_helper_network

  core-api-phleb:
    container_name: core-api-phleb
    ports:
      - "8992:80"
    build: ./core-api
    environment:
      - UWSGI_INI=uwsgi.ini
      - PRE_START_PATH=/usr/src/phlebology-helper-backend/prestart.sh
      - DATABASE_URL=postgresql://phlb_viewer:phlb_viewer@130.193.52.22:5432/phlb_viewer
    networks:
      - phlebology_helper_network



networks:
  phlebology_helper_network:
    driver: bridge